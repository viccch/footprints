<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!--    <style name="Base.Theme.footprints" parent="Theme.Material3.DayNight.NoActionBar">-->
    <style name="Base.Theme.footprints" parent="Theme.Material3.DayNight">
        <item name="actionBarSize">40dip</item>
    </style>

    <style name="Theme.footprints" parent="Base.Theme.footprints" />

    <style name="Theme.footprints.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.footprints.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.footprints.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.footprints.Fullscreen" parent="Theme.footprints">
        <item name="android:actionBarStyle">@style/Widget.Theme.footprints.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

</resources>